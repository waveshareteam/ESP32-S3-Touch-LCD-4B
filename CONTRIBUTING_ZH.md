# 贡献指南

[English](CONTRIBUTING.md) | [简体中文](CONTRIBUTING_ZH.md)

欢迎改进 ESP32-S3-Touch-LCD-4B 的硬件资料、示例、固件文档和验证。

## 提交拉取请求前

1. 会改变硬件行为、公开路径、固件格式或框架支持时，先创建 Issue。
2. 每次修改保持聚焦，公开文本只使用仓库相对路径。
3. 改动引脚、显示/触摸、电源、存储、USB、音频或其他板级行为时附硬件证据。
4. 在仓库根目录运行 `python scripts/check_repository.py` 和静态测试。
5. 确保提交后的 Actions 为所有路由到的第一方示例完成；硬件修改还应在对应修订上实测。

## 示例

- ESP-IDF 工程放在 `examples/esp-idf/<example-name>/`。
- Arduino 草图放在 `examples/arduino/examples/<example-name>/`。
- 随包第三方库内部示例不得加入产品 CI 矩阵。
- 可复用 BSP、显示、触摸、传感器、音视频或总线驱动优先使用托管组件。

每个示例 README 应说明目标、框架版本、依赖、配置、构建/烧录方式、硬件修订和预期输出。

## 硬件与公开文本

文件名或邻近元数据应标识硬件修订；原理图、PCB、生产文件、机械图、引脚表和预览必须
一致。专有源格式应尽量同时提供 PDF、图片或中性交换格式。

分支和提交名保持简洁、中性。公开文本不得包含用户名、本地绝对路径、盘符、网络路径、
IDE 状态或机器专属命令。
